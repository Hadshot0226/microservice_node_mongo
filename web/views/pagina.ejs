<!DOCTYPE html>
<html>

<head>
    <title><%= title %></title>
    <meta charset="utf-8" />
</head>

<body>
    <% if(exibirDiv) { %>
        <div>Apenas uma div</div>
    <% } else { %>
        div>Apenas outra div</div>
    <% } %>
</body>

</html>